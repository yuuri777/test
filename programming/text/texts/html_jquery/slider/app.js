$(function() {
    $(".slider").slick({
        autoplay: true,
        //スライダーを自動でスライドするかのコード
        autoplayspeed: 2000,
        //自動スライドの感覚
        dots: true,
        //画像下のドットを表示するかしないか
        //slickで検索
    });
});